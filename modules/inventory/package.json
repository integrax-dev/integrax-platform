{
  "name": "@integrax/module-inventory",
  "version": "0.1.0",
  "type": "module",
  "description": "Inventory management — stock sync, reservation, low-stock alerts",
  "main": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "build": "tsc --noEmit", "test": "vitest run" },
  "dependencies": {
    "@integrax/entities": "workspace:*",
    "@integrax/event-bus": "workspace:*",
    "@integrax/snapshot-store": "workspace:*",
    "@integrax/platform-kernel": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
