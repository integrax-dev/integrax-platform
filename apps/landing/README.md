# landing — `@integrax/landing`

Public marketing landing page for IntegraX. Built with React 18, React Router, and Vite.

No API dependencies. Static build output. No authentication.

**Dev:** `pnpm dev` | **Build:** `pnpm build`
