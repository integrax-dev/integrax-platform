# @integrax/connector-mobbex

Connector for Mobbex payment gateway. Scaffold implementation — verify endpoint paths and payload shapes against Mobbex developer docs before production use.

**Auth:** `x-api-key` + `x-access-token` headers.

**Actions:** create checkout, get transaction, refund.

**Consumers:** `modules/payments`.
