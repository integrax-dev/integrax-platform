{
  "name": "@integrax/ontology",
  "version": "0.1.0",
  "description": "Pure data: synonym pairs, dictionary sets, business-type providers and connector seeds — no engine logic",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
