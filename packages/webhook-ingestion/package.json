{
  "name": "@integrax/webhook-ingestion",
  "version": "0.1.0",
  "type": "module",
  "description": "Webhook signature validation, payload normalization and event-bus enqueue",
  "main": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@integrax/event-bus": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.1"
  }
}
