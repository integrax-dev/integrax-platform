{
  "name": "@integrax/platform-kernel",
  "version": "0.1.0",
  "type": "module",
  "description": "Schema-agnostic diff engine and cross-connector identity resolution",
  "main": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@integrax/entities": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.1"
  }
}
