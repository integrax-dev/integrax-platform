{
  "name": "@integrax/reconciliation-engine",
  "version": "0.1.0",
  "type": "module",
  "description": "Canonical entity reconciliation engine — identity matching, diff detection, policy actions",
  "main": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.1"
  }
}
