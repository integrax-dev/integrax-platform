{
  "name": "@integrax/timeline",
  "version": "0.1.0",
  "type": "module",
  "description": "Append-only event log for entity changes, sync runs, conflict lifecycle, and workflow executions",
  "main": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "build": "tsc --noEmit" },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
