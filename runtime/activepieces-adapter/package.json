{
  "name": "@integrax/activepieces-adapter",
  "version": "0.1.0",
  "description": "Activepieces runtime adapter — compiles IntegraxFlow to Activepieces JSON and executes flows",
  "type": "module",
  "main": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@integrax/workflow-engine": "workspace:*",
    "@integrax/integration-engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.1"
  }
}
